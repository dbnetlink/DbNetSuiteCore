@page
@using DbNetSuiteCore.Enums.DbNetGrid;
@using DbNetSuiteCore.Components;
@{
    // Configure the grid and render
    DbNetGridCore productsGrid = new DbNetGridCore("northwind","products");
    productsGrid.SetColumnProperty("discontinued", ColumnPropertyType.DataType, nameof(Boolean));
    @productsGrid.Render()
}