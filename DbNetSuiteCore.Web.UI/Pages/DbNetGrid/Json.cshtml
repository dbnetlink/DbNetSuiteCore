@page
@using DbNetSuiteCore.Components;
@{
    /*
    DbNetGridCore customersGrid = new DbNetGridCore("/data/customers.json", "customers");
    customersGrid.PageSize = 10;
    @customersGrid.Render()
    */

    DbNetGridCore ordersGrid = new DbNetGridCore("/data/orders.json", "orders");
    ordersGrid.Column(new string[] { "orderdate", "requireddate", "shippeddate" }).DataType(typeof(DateTime));
    ordersGrid.PageSize = 10;
    @ordersGrid.Render()
}