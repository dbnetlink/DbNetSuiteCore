@page
@using DbNetSuiteCore.Enums.DbNetGrid;
@using DbNetSuiteCore.Components;
@using DbNetSuiteCore.Web.UI.Enums;

@{
    DbNetGridCore territoriesGrid = new DbNetGridCore("northwind", "territories");
    territoriesGrid.Update = true;
    territoriesGrid.Column("RegionId").Lookup(typeof(RegionEnum)).Label("Region");
    @territoriesGrid.Render()
}