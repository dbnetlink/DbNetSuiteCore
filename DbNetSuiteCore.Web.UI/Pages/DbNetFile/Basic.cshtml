@page
@using DbNetSuiteCore.Components;
@using DbNetSuiteCore.Enums;
@{
    DbNetFileCore folderView = new DbNetFileCore("/dbnetfile/documents/sampleimages");
    //folderView.Caption = "This is an example caption";
    @folderView.Render()
}