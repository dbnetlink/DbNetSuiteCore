@namespace DbNetSuiteCore.Views.DbNetGrid
@using DbNetSuiteCore.ViewModels.DbNetCombo;
@using System.Data
@model ComboViewModel

@{
    if (Model.AddFilter)
    {
        <input class="dbnetcombo" type="text" />
    }
    <select class="dbnetcombo"></select>
}